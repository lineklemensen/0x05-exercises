#include "strings.h"

int main() {
  
  // Exercise 0x02
  letter_seen_twice("foobarbaz");

  return 0;
}

// Exercise 0x01
std::string letter_seen_twice(std::string text) {
  // Loop over each character in 'text' and register each character
  // If character has been seen return character
  
  // Hint: You'll probably want a nested for-loop (one for the current character and one for the characters you've seen before)
  return "";
}